// Converta PDFs/IMGs para pedaços adequados (ex.: base64/png ou OCR prévio)
export async function prepareFilesForVision(_fileUrls: string[]) {
    // TODO: implementar conforme seu fluxo de upload
    return [] as { type: "input_image"; image_url: string }[];
    }